# DEPRECATED — Use pyproject.toml instead
# ========================================
#
# All dependencies are now declared in pyproject.toml.
#
# Install core:     pip install -e .
# Install dev:      pip install -e ".[dev]"
# Install datasets: pip install -e ".[datasets]"
# Install all:      pip install -e ".[dev,datasets,toon,graph,sandbox,tuning,ui]"
#
# This file is kept only for backwards compatibility with scripts
# that may reference it. Do not add new dependencies here.

# Minimal subset for scripts that use `pip install -r requirements-dev.txt`
jsonschema>=4.20
pyyaml>=6.0
pytest>=7.4.0
pytest-asyncio>=0.23.0
