# Progress Log: 2026-01-16

## Engram Tokenizer Compression Experiment (COMPLETE - Negative Result)

Tested whether Engram-style tokenizer compression (from DeepSeek's Jan 2025 paper) could improve Prompt Lookup N-gram match rates.

### Background

**Source**: "Conditional Memory via Scalable Lookup: A New Axis of Sparsity for LLMs" (DeepSeek-AI, Jan 2025)

Engram's `CompressedTokenizer` achieves 23% vocabulary reduction by normalizing semantically equivalent tokens:
- NFKC + NFD Unicode normalization
- Strip accents
- Lowercase
- Collapse whitespace

**Hypothesis**: Applying this normalization during Prompt Lookup matching could catch additional N-gram matches (e.g., "Alexander" = "ALEXANDER" = "alexander").

### Experiment Design

Created `scripts/experiments/compressed_tokenizer_analysis.py` to measure:
1. Build N-gram index with raw token IDs (current Prompt Lookup behavior)
2. Build N-gram index with normalized token text
3. Count additional speculatable positions from compression

### Results

| Test Case | 3-gram | 4-gram | 5-gram | Verdict |
|-----------|--------|--------|--------|---------|
| Case variations ("Alexander"/"ALEXANDER") | 0% | 0% | 0% | No benefit |
| Code naming (getUserData/get_user_data) | +42.9% | +200% | 0% | Only code patterns |
| Exact phrase repeats | 0% | 0% | 0% | Expected |
| Documentation headings | +50% | 0% | 0% | Minor |
| Technical docs (realistic) | 0% | 0% | 0% | No benefit |

### Why It Doesn't Work

1. **BPE tokenization encodes case differently**: "Alexander" and "ALEXANDER" become completely different token ID sequences at the byte level
2. **Text normalization can't unify tokens split differently**: Normalizing decoded text doesn't help when the underlying byte sequences differ
3. **Code naming is the exception**: camelCase vs snake_case sometimes share normalized forms, but absolute gains are tiny (7→10 positions)

### Key Insight

Engram's tokenizer compression works because they **retrained the tokenizer** to emit the same token IDs for normalized equivalents. Applying normalization at inference time to an existing BPE tokenizer cannot achieve this.

### Conclusion

**Do NOT pursue llama.cpp modifications** - the approach is fundamentally limited by tokenizer architecture. Phase 2 (implementation) not warranted.

### Files

| File | Description |
|------|-------------|
| `scripts/experiments/compressed_tokenizer_analysis.py` | Experiment script (preserved) |
| `research/SPECULATIVE_DECODING_REPORT.md` | Added finding to "What Doesn't Work" section |

### Documentation Updates

- Updated `research/SPECULATIVE_DECODING_REPORT.md` Section 8.2 item #6
- Deleted completed handoff `handoffs/active/compressed_tokenizer_prompt_lookup.md`

---

## K-Value Optimization Analysis (COMPLETE)

Analyzed 76 model+draft combinations across K={4,8,16,24} to understand optimal K (draft_max) parameter selection.

### Key Findings

| Category | Count | Percentage |
|----------|-------|------------|
| K=24 optimal (still increasing) | 18 | 24% |
| K<=8 optimal | 47 | 62% |
| K=16 optimal | 11 | 14% |

### Pattern: Model Size Determines Optimal K

**Large dense (70B+)**: ALL show monotonically increasing performance up to K=24:
- Llama-3.1-70B + PARD-1B-Q8: 4.7 → 10.3 t/s (K=4 → K=24)
- Qwen2.5-7B + Coder-0.5B: 22.8 → 46.6 t/s (K=4 → K=24) - 2x gain!

**MoE models**: ALL peak at K=8:
- Qwen3-Coder-30B + 0.75B: 31.4 (K=8) → 14.0 (K=24) - DECREASING

**Small thinking models**: Peak at K=4 or K=8

### Theoretical Explanation

Optimal K = f(target_inference_time / draft_inference_time)
- Large models: Target slow, draft overhead negligible → higher K better
- MoE/small: Target fast, draft overhead dominates at high K

### Recommendations

1. For 70B+ dense models: Test K={8,16,24,32,48}
2. For MoE models: Only test K={4,8}
3. For small dense: Only test K={4,8,16}

### Priority Models for K>24 Testing

1. `general_qwen2_5_7b + coder_0_5b`: 46.6 t/s at K=24 (still climbing)
2. `worker_summarize + qwen25_coder`: 21.3 t/s at K=24 (jumped from 8.0)

### Files Created

| File | Description |
|------|-------------|
| `research/K_VALUE_OPTIMIZATION.md` | Full analysis with data tables |

### Status

- Analysis: COMPLETE
- Empirical testing of K=32/48: BLOCKED (need model slot availability)

---

## Aider CLI Integration (COMPLETE)

Successfully integrated Aider as the terminal CLI for the orchestrator, enabling IDE-like coding assistance via our local LLM infrastructure.

### Problem Solved

The `/v1/chat/completions` endpoint was returning empty responses - it only had mock stub code that was never wired to real orchestration.

### Changes Made

**1. Fixed OpenAI-compat endpoint** (`src/api/routes/openai_compat.py`):
- Added real orchestration support (REPL execution, Root LM loop)
- Added mock mode fallback when llama-server not running
- Respects `features().mock_mode` flag for testing

```python
# Key fix: Check feature flag for mock mode
from src.features import features
f = features()
use_real_mode = (
    state.registry is not None
    and not f.mock_mode  # Respect mock_mode feature flag
)
```

**2. Aider Installation** (Python 3.13 workaround):
- Aider requires Python 3.12 (`pkgutil.ImpImporter` removed in 3.13)
- Solution: Use `uv` package manager to install Python 3.12

```bash
# Install uv and Python 3.12
curl -LsSf https://astral.sh/uv/install.sh | sh
uv python install 3.12
uv venv --python 3.12 /tmp/aider-env
uv pip install --python /tmp/aider-env/bin/python aider-chat
```

**3. Configuration** (`~/.aider.conf.yml`):
```yaml
model: openai/orchestrator
openai-api-base: http://localhost:8000/v1
openai-api-key: dummy
auto-commits: false
stream: true
```

### Test Results

```bash
$ echo "What is 2+2?" | /tmp/aider-env/bin/aider --no-git --yes --message "What is 2+2?"
Model: openai/orchestrator with whole edit format
[MOCK] Processed via frontdoor: What is 2+2?
Tokens: 598 sent, 36 received.
```

Both streaming and non-streaming modes verified working.

### Files Modified

| File | Change |
|------|--------|
| `src/api/routes/openai_compat.py` | Full orchestration, mock fallback, ~150 lines |
| `handoffs/active/orchestrator-ui.md` | Updated Quick Resume with working instructions |
| `progress/2026-01/2026-01-15.md` | Added Aider integration section |

### Architecture

```
┌─────────────────────────────────────────┐
│  Aider CLI (Python 3.12 venv)           │
│  - Terminal UX, git integration         │
│  - Repomap for codebase context         │
└─────────────────┬───────────────────────┘
                  │ /v1/chat/completions
                  ▼
┌─────────────────────────────────────────┐
│  Orchestrator API                       │
│  - Mock mode (llama-server offline)     │
│  - Real orchestration (llama-server on) │
│  - MemRL routing when enabled           │
└─────────────────────────────────────────┘
```

### Next Steps (Production Testing)

- [ ] Test with live llama-server instances
- [ ] Test tool calling passthrough
- [ ] Test multi-turn escalation with Aider's `/architect` mode

---

## Phase 5 Documentation Updates (COMPLETE)

Completed documentation updates from previous session:

### Escalation Triggers Added

| Trigger | Effect | Model | Use Case |
|---------|--------|-------|----------|
| `/architect`, `/plan` | Forces COMPLEX | architect_general | Task decomposition |
| `/think`, `ultrathink` | Routes to thinking model | thinking_reasoning | Deep reasoning |

**Key distinction:**
- Architect escalation: Changes WHAT happens (task breakdown)
- Thinking escalation: Changes HOW it happens (deeper reasoning)

### Complexity-Aware Routing

| Complexity | Action | Example |
|------------|--------|---------|
| TRIVIAL | Frontdoor answers directly | "What's 2+2?" |
| SIMPLE | REPL execution (no architect) | "List files in src/" |
| MODERATE | Single specialist delegation | "Write a quicksort function" |
| COMPLEX | Architect generates TaskIR | "Design Byzantine consensus" |

### Files Updated

- `handoffs/active/orchestration-refactoring.md` - Session 7 with escalation triggers
- `progress/2026-01/2026-01-15.md` - Complexity routing and token efficiency sections
