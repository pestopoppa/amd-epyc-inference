# Progress Log: 2026-01-29

## Summary

**MemRL-Informed Model Routing** — gave models agency in routing decisions. Models can now self-assess task complexity using MemRL Q-values and either escalate UP to larger models or delegate DOWN to workers/specialists.

---

## Problem Statement

Models had **zero routing agency**. All escalation/delegation was hardcoded Python control flow:

1. **`escalate()` was dead code** — set `artifacts["_escalation_requested"]` but `chat.py` never checked it
2. **`recall()` was wasteful** — created fresh `EpisodicStore` + `TaskEmbedder` per call, returned no Q-values
3. **No model self-awareness** — models didn't know their role, tier, or capabilities
4. **No MemRL visibility** — rich Q-value infrastructure (episodic memory, failure graphs, hybrid router) existed but was invisible to models

## Solution

Exposed MemRL intelligence to the REPL environment via 5 model-facing functions, wired artifact checking into chat.py, and injected routing context on turn 0.

---

## Implementation

### Files Modified (4)

| File | Changes | LOC |
|------|---------|-----|
| `src/repl_environment.py` | Fixed `_recall`, `_escalate`; added `_my_role`, `_route_advice`, `_delegate` | +530 |
| `src/escalation.py` | Added `DELEGATE` action, `target_role_requested` field, `should_delegate` property | +17 |
| `src/prompt_builders.py` | Updated tool/rules docs, added `build_routing_context()`, `routing_context` param | +143 |
| `src/api/routes/chat.py` | Pass MemRL to REPL, check routing artifacts, streaming parity, routing context injection | +392 |

### New REPL Functions

| Function | Purpose | Key Feature |
|----------|---------|-------------|
| `my_role()` | Self-awareness | Returns role, tier, capabilities, delegation targets |
| `route_advice(task)` | MemRL routing | Queries HybridRouter, returns Q-values + recommended role |
| `delegate(prompt, role, reason)` | Tracked delegation | Outcome recording, Tier C guard, MemRL learning |
| `escalate(reason, target)` | Targeted escalation | Can specify target role (not just next-in-chain) |
| `recall(query)` | Fixed episodic memory | Reuses shared retriever, returns Q-values + best_action |

### Data Flow (Before → After)

```
BEFORE:
  Request → HybridRouter (pre-model) → Model executes → Error? → EscalationPolicy → retry/escalate
  Model has: recall() [broken], escalate() [dead], llm_call() [blind]

AFTER:
  Request → HybridRouter (pre-model) → Inject routing context → Model executes
  Model can: my_role() → route_advice(task) → delegate(prompt, role) or escalate(reason, target)
  After execute: chat.py checks artifacts → honors model's routing request
  MemRL logs delegation outcomes → Q-values improve future routing
```

### Tier Guard

Workers (Tier C) cannot delegate — enforced in `_delegate()`:
```
[ERROR: Workers (Tier C) cannot delegate to other models. Use TOOL() for deterministic tools or FINAL() to return results.]
```

Workers can still use all deterministic tools (`TOOL()`, `run_shell()`, `peek()`, etc.).

### Streaming Endpoint Parity

The streaming endpoint (`/chat/stream`) now has full routing parity with the main endpoint:
- Escalation state tracking (`current_role`, `role_history`, `consecutive_failures`)
- Routing context injection on turn 0
- Model-initiated escalation via artifacts
- Delegation outcome logging
- Error-based escalation via EscalationPolicy

---

## Implementation Steps (All Complete)

| Step | Description | Status |
|------|-------------|--------|
| 1 | Fix `_recall()` — reuse MemRL retriever, expose Q-values | ✅ |
| 2 | Fix `_escalate()` — add `target_role` parameter | ✅ |
| 3 | Add `_my_role()` — model self-awareness | ✅ |
| 4 | Add `_route_advice()` — MemRL routing recommendations | ✅ |
| 5 | Add `_delegate()` — tracked delegation with tier guard | ✅ |
| 6 | Pass MemRL to REPL in `chat.py` (both endpoints) | ✅ |
| 7 | Check routing artifacts after `execute()` | ✅ |
| 7B | Apply routing to streaming endpoint | ✅ |
| 8 | Add `DELEGATE` action to escalation module | ✅ |
| 9 | Update prompt builders (tools, rules, routing context) | ✅ |
| 9D | Wire `build_routing_context()` into turn 0 | ✅ |
| 10 | Verify all changes (AST + import + behavioral) | ✅ |

---

## Verification Results

### AST Parse Check
```
  OK: src/repl_environment.py
  OK: src/escalation.py
  OK: src/prompt_builders.py
  OK: src/api/routes/chat.py
All 4 files parse successfully.
```

### Import Checks
```
escalation.py: DELEGATE exists, target_role_requested field works, should_delegate property works
prompt_builders.py: build_routing_context signature correct, routing_context param in build_root_lm_prompt
repl_environment.py: retriever + hybrid_router constructor params, all 5 methods exist
chat.py: All imports resolve correctly
```

### Behavioral Checks
```
_escalate("Task too complex", target_role="coder_primary") → sets correct artifacts ✅
_escalate("Need help") → sets artifacts without target ✅
_my_role() → {"role": "frontdoor", "tier": "A", "capabilities": [...], "can_delegate_to": [...]} ✅
_route_advice("test") → {"strategy": "unavailable"} when no router ✅
_delegate() from Tier A → ALLOWED, delegation recorded ✅
_delegate() from Tier C → BLOCKED with error message ✅
```

---

## Design Decisions

1. **Streaming parity now**: Both `/chat` and `/chat/stream` have identical routing logic
2. **Tier C guard in `_delegate()`**: Workers cannot delegate. They can use deterministic tools.
3. **No cold-start bootstrapping**: MemRL is pre-seeded (2,714 memories, 13 failure modes). Learns continuously via TD-learning.
4. **Routing context capped at 300 chars**: ~75 tokens overhead on turn 0, negligible impact
5. **Legacy `recall()` fallback**: When retriever is None, creates fresh instances (backward compat)

---

## Files Created/Modified

| File | Status |
|------|--------|
| `src/repl_environment.py` | MODIFIED |
| `src/escalation.py` | MODIFIED |
| `src/prompt_builders.py` | MODIFIED |
| `src/api/routes/chat.py` | MODIFIED |

---

---

## Orchestrator Benchmark Fixes (7 Steps)

The ORCHESTRATOR_STACK_20260129 benchmark scored **54%** (44/81). Root cause analysis identified 6 failure categories. All 7 fixes implemented and verified.

### Problem Summary

| Issue | Root Cause | Impact |
|-------|-----------|--------|
| Output capture | `chat.py` uses `result.final_answer` only, discards `result.output` | "Analysis complete. See above." — printed content lost |
| Routing telemetry | `ChatResponse` has no `routed_to` field; benchmark defaults to `"unknown"` | Can't debug routing; all telemetry zeros |
| VL empty | `_handle_vision_request` calls `/vision/analyze` → 404 (route is at `/v1/vision/analyze`) | 1/12 VL score |
| Long context | Model generates standalone Python instead of calling REPL builtins (`peek`, `grep`, `summarize_chunks`) | 0/9 LC score |
| Agentic empty | Model generates `delegate("...", "researcher_agent")` — role doesn't exist | 5 empty agentic responses |
| No speed metrics | Benchmark captures `latency_ms` but not `tokens_used` from API | Can't compute t/s |

### Files Modified (6)

| File | Changes | LOC |
|------|---------|-----|
| `src/api/routes/chat.py` | Output capture, routing fields, VL URL fix + fallback, two-stage generalization, MemRL review gate | +180 |
| `src/api/models/responses.py` | `routed_to`, `role_history`, `routing_strategy`, `tokens_generated` fields | +4 |
| `src/escalation.py` | `REVIEW` action in `EscalationAction` enum | +1 |
| `src/repl_environment.py` | Role alias mapping + resolution in `_delegate()` and `_escalate()` | +25 |
| `src/prompt_builders.py` | LC exploration prompt, review verdict/revision prompts, TOON routing context | +120 |
| `scripts/benchmark/compare_orchestrator_direct.py` | Speed metrics extraction (`tokens_generated`, `avg_tps`) | +30 |

### Step-by-Step Implementation

| Step | Description | Fix |
|------|-------------|-----|
| 1 | Output capture | `_resolve_answer()` — detects stub FINAL args ("complete", "see above"), uses captured stdout |
| 2 | Routing telemetry | 4 new `ChatResponse` fields + populated in all 4 return paths + benchmark extraction |
| 3 | VL pipeline | URL `/vision/analyze` → `/v1/vision/analyze` + fallback context injection |
| 4 | Long context | Generalized two-stage to ALL contexts >20K chars; worker parallel digest + frontdoor synthesis |
| 5 | Role aliases | `_ROLE_ALIASES` maps `researcher_agent`→`worker_explore`, etc.; applied in delegate + escalate |
| 6 | MemRL review gate | Two-phase: architect verdict (80 tokens, ~6s) → worker revision (44 t/s) when Q<0.6; TOON-encoded digests |
| 7 | Speed metrics | `ComparisonResult.avg_tps` = `tokens_generated / server_elapsed_s` |

### Two-Phase Review Gate Design

```
After FINAL(answer):
  1. Check MemRL Q-value for role+task
  2. If Q < 0.6 → architect verdict (~40 tokens at 6.75 t/s → ~6s)
  3. If "WRONG: <fix>" → worker revision (~500 tokens at 44 t/s → ~11s)
  4. Net: ~1.9s average added latency (20% trigger × (6s + 30% × 11s))
```

### TOON Integration (Routing Context)

`build_routing_context()` now TOON-encodes the MemRL similar-tasks array when ≥2 results found. Estimated 50% reduction (~40-60 tokens saved per request turn 0).

### Verification

- AST parse: all 6 files OK
- pytest: 888/889 pass (1 pre-existing failure in `tests/vision/test_api.py` — mocks non-existent `get_session`)
- TOON architecture review: 5 integrations already active, 1 new (routing context), 2 unused functions identified

---

## Next Steps

1. Restart orchestrator stack and re-run benchmark to verify fixes
2. Monitor MemRL Q-value updates from delegation outcomes
3. Activate unused TOON functions: `encode_memory_results()`, `encode_procedures()`
4. Tune routing context format based on model comprehension
5. Consider adding routing telemetry dashboard (delegation success rates per role)
