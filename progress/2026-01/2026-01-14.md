# Progress Log: 2026-01-14

## Memory Crash Fix

### Incident

Previous agent session (~00:52) crashed the machine while running orchestration tests. The agent was likely running pytest to validate existing code before implementing their planned changes (Tool/Script Registry wiring).

**Observed**: Multiple pytest processes visible in htop, consuming all available RAM.

**Root cause**: `src/api.py` eagerly initialized MemRL components (TaskEmbedder, QScorer, HybridRouter) at API startup, even for `mock_mode=True` tests. With pytest-xdist `-n auto` on a 192-thread machine, ~192 workers spawned, each loading the 0.5B embedding model.

### Fix Applied

| Component | Change |
|-----------|--------|
| **Lazy MemRL Loading** | Added `_memrl_initialized` flag; components only load on first `real_mode=True` request |
| **Memory Guard** | `tests/conftest.py` fails if <100GB free RAM, warns about `-n auto` on 192-thread machine |
| **Safe Parallelism** | Added warning comment to `pyproject.toml` about max 4 workers |
| **Pre-test Check** | `make check-memory` target, `test-all` depends on it |
| **Documentation** | Added "Test Memory Safety" section to CLAUDE.md |

### Files Modified

- `src/api.py` - Lazy MemRL initialization
- `tests/conftest.py` - NEW - Memory guard and marker registration
- `pyproject.toml` - psutil dependency, parallelism warning
- `Makefile` - `check-memory` target
- `CLAUDE.md` - Test safety documentation

### Verification

All 26 API tests pass. Mock mode tests no longer trigger model loading.

---

## Previous Agent's Planned Work (Not Started)

The crashed agent's plan file: `/home/daniele/.claude/plans/glowing-splashing-eclipse.md`

### Task A: Wire Tool/Script Registries

Goal: Enable `TOOL()`, `SCRIPT()`, `find_scripts()` in REPL

Changes planned:
1. Add `tool_registry` and `script_registry` to AppState
2. Initialize in `lifespan()` startup
3. Pass to REPLEnvironment constructor
4. Create `orchestration/script_registry/` directory

### Task B: Role-Based Generation Defaults

Goal: Concise responses from heavy models via role-specific settings

Changes planned:
1. Add `generation_defaults` to role definitions in `model_registry.yaml`
2. Add `system_prompt_suffix` for architect roles ("Be maximally concise...")
3. Extend `RoleConfig` dataclass in `registry_loader.py`
4. Apply defaults in `llm_call()` based on role

**Status**: Ready to implement. All code analysis was complete, plan was finalized.

---

## MemRL Integration Status Update

Per the Jan 13 progress report, MemRL Phases 2-3 are **COMPLETE**:

### Phase 2: Wire Logging (COMPLETE)
- [x] ProgressLogger wired into dispatcher (`src/dispatcher.py`)
- [x] Log routing decisions in API (`src/api.py`)
- [x] Log gate results in GateRunner (`src/gate_runner.py`)

### Phase 3: Enable Hybrid Routing (COMPLETE)
- [x] HybridRouter integration (`src/dispatcher.py`, `src/api.py`)
- [x] Q-Scorer integration (real-time + idle cleanup)
- [x] Confidence logging

**Note**: `orchestration/BLOCKED_TASKS.md` was outdated and showed these as unchecked. Updated today.

---

## Lazy Loading Architecture

The MemRL lazy loading follows the HOT/WARM/COLD memory pool design from `research/ESCALATION_FLOW.md`:

| Tier | Components | Loading |
|------|------------|---------|
| HOT (~35GB) | Frontdoor, draft models | Always resident |
| WARM (~460GB) | TaskEmbedder, specialists | Load on demand via mmap |
| COLD | Archived models | Disk only |

TaskEmbedder is now correctly in the WARM tier - only loaded when actual Q-scoring is needed, not on every API startup.

---

## Safe Test Commands

```bash
# Check memory before running tests
make check-memory

# Safe parallel execution (max 4 workers)
pytest tests/ -n 4

# Run all tests with memory check
make test-all

# DANGER: Never use on 192-thread machine
# pytest tests/ -n auto  # <-- Will spawn ~192 workers!
```

---

## Role-Based Generation Defaults (COMPLETE)

Implementation of the previous agent's planned Task B (from `glowing-splashing-eclipse.md`).

### Summary

Added `generation_defaults` and `system_prompt_suffix` to key roles in `model_registry.yaml`:

| Role | n_tokens | temperature | system_prompt_suffix |
|------|----------|-------------|---------------------|
| frontdoor | 1024 | 0.3 | "Provide clear, user-friendly explanations..." |
| coder_primary | 2048 | 0.2 | "Output code only..." |
| architect_general | 512 | 0.1 | "Be maximally concise..." |
| architect_coding | 512 | 0.1 | "Be maximally concise..." |
| ingest_long_context | 4096 | 0.2 | "Synthesize key points..." |
| thinking_reasoning | 4096 | 0.3 | "Think step by step..." |
| coder_escalation | 2048 | 0.2 | "Output code only..." |
| worker_general | 1024 | 0.3 | None |
| worker_math | 512 | 0.0 | "Show work step by step..." |
| worker_summarize | 2048 | 0.2 | "Produce concise summaries..." |
| toolrunner | 512 | 0.2 | "Summarize tool output briefly..." |

### Files Modified

- `orchestration/model_registry.yaml` - Added generation_defaults to 5 roles
- `src/api.py` - Registry loading at startup for mock mode

### Previous Implementation (Already Done)

A previous agent already implemented:
- `GenerationDefaults` dataclass in `registry_loader.py`
- `get_role_defaults()` method
- Role defaults application in `llm_primitives.py`
- Defaults for frontdoor, coder_primary, architect_general, architect_coding, worker_general, worker_math

### Test Results

All 77 tests pass:
- 26 API tests
- 20 registry loader tests
- 31 LLM primitives tests

---

## References

- Crashed agent's plan: `/home/daniele/.claude/plans/glowing-splashing-eclipse.md`
- Memory pool architecture: `research/ESCALATION_FLOW.md`
- MemRL handoff: `handoffs/active/memrl-episodic-memory.md`
- RLM roadmap: `handoffs/active/rlm-orchestrator-roadmap.md`
