# Progress Report: 2026-02-07

## Infra Stabilization for Seeding + CPU-Exclusive Inference

### Summary
Implemented core safeguards to prevent seeding regressions caused by event-loop blocking and concurrent heavy-model inference. Added cross-process locking to serialize heavy models, offloaded blocking calls from async handlers, improved health probes, and hardened 3-way seeding recovery/cleanup.

### Changes Made

#### 1. CPU-Exclusive Inference Lock
- New cross-process lock (`src/inference_lock.py`) enforcing exclusive heavy-model inference and shared lock for workers/embedders.
- Integrated lock into LLM backend calls and embedding pipeline.

#### 2. Async Safety
- Offloaded blocking LLM calls via `asyncio.to_thread()` in chat pipeline and REPL orchestration.
- Added graceful fallback when generation monitoring is enabled but caching backends are used.

#### 3. Seeding Infra Hardening
- Added slot cleanup on infra timeouts for 3-way mode.
- Triggered recovery attempts when all roles fail due to infra in 3-way evaluation.
- Updated heavy ports set to include frontdoor/coder escalation.

#### 4. Health & Uvicorn
- Added backend liveness probes to `/health` response.
- Enabled uvicorn workers via `ORCHESTRATOR_UVICORN_WORKERS` (default 2).

#### 5. Documentation
- Updated seeding chapter with infra safeguards summary.
#### 6. Embedder + Memory Fixes
- Embedder HTTP response parsing now accepts list payloads (llama-server returns list wrapper).
- Backfill script now updates SQLite `embedding_idx` to match rebuilt FAISS indices.
- Retriever now tolerates FAISS entries without in-memory embeddings (uses precomputed similarity score).
- Reset script aligns API restart env with seeding infra and derives embedding dim from config.
- Docs and session store defaults updated to reflect BGE-large (1024-dim) embedder.
- Session embeddings now auto-recreate if stored dim mismatches the configured dimension.
- Launch script now reflects BGE embedder + fanout ports.
- `scripts/migrate_to_faiss.py` compare dim updated to 1024.

### Files Added
| File | Purpose |
|------|---------|
| `src/inference_lock.py` | Cross-process heavy-model serialization lock |

### Files Modified
| File | Change |
|------|--------|
| `src/llm_primitives/inference.py` | Heavy lock + role timeout enforcement |
| `src/api/routes/chat.py` | Offload blocking stages to threads |
| `src/api/routes/chat_pipeline/repl_executor.py` | Async-safe LLM calls + monitor fallback |
| `src/api/routes/chat_pipeline/stages.py` | Async-safe proactive planning call |
| `src/api/routes/health.py` | Core backend probes |
| `src/api/models/responses.py` | Added backend_probes field |
| `orchestration/repl_memory/embedder.py` | Shared lock for embeddings |
| `scripts/benchmark/seed_specialist_routing.py` | 3-way slot cleanup + recovery |
| `scripts/benchmark/seeding_types.py` | Expanded heavy ports |
| `scripts/benchmark/seeding_infra.py` | Uvicorn workers in API restart |
| `scripts/server/orchestrator_stack.py` | Uvicorn workers default |
| `docs/chapters/17-memory-seeding.md` | Infra safeguards section |
| `orchestration/repl_memory/embedder.py` | Accept list payload embedding responses |
| `orchestration/repl_memory/parallel_embedder.py` | Accept list payload embedding responses |
| `orchestration/repl_memory/retriever.py` | Handle `memory.embedding=None` |
| `scripts/backfill_faiss_embeddings.py` | Update SQLite `embedding_idx` during backfill |
| `scripts/session/reset_episodic_memory.sh` | Safer API restart + env parity + dynamic embedding dim |
| `src/session/sqlite_store.py` | Session embedding dim default set to 1024 |
| `src/session/protocol.py` | Updated embedding dim docs |
| `src/features.py` | Updated TaskEmbedder docstring |
| `docs/chapters/04-storage-and-safety.md` | Updated embedder model note |
| `docs/chapters/15-memrl-system.md` | Updated embedder model + dim |
| `docs/chapters/17-memory-seeding.md` | Updated embedder model note |
| `docs/chapters/20-session-persistence.md` | Updated embedding dim size |
| `orchestration/README.md` | Updated embedder model note |
| `orchestration/BLOCKED_TASKS.md` | Updated embedder model note |
| `scripts/server/launch_production.sh` | Embedder model + fanout note |
| `scripts/migrate_to_faiss.py` | Compare dim set to 1024 |

### Test Results
- Not run (infra-only changes; recommend targeted smoke + 3-way dry run).

### Next Steps
- Run `/chat` smoke test + 3-way dry run to verify no event-loop blocking.
- Validate heavy lock behavior under parallel API requests.

### Checks Run
- One-shot API bring-up (local uvicorn, workers=1) and smoke test:
  - `/health` OK; backend probes for `frontdoor`, `coder_escalation`, `architect_general`, `architect_coding` all OK.
  - `/chat` smoke test (`2+2`) **OK** with real_mode, routed to `coder_escalation`, ~1.1s.
  - Embedder/FAISS mismatch resolved by rebuilding FAISS (1024-d) and fixing response parsing.
  - Log: `/tmp/orchestrator_tmp.log`.

---

## Pydantic Ecosystem Research (Session 2)

### Summary
Analyzed four Pydantic-related repositories for potential integration with orchestration infrastructure. Discovered and fixed a bug in MemRL escalation target parsing. Created comprehensive handoff documenting migration path to explicit graph-based workflows.

### Repositories Analyzed

| Repository | Relevance | Recommendation |
|------------|-----------|----------------|
| `pydantic/pydantic-ai` (Graph module) | HIGH | Migrate to explicit graphs |
| `pydantic/pydantic-ai` (Agent framework) | MEDIUM | Adopt DI pattern |
| `pydantic/monty` | MEDIUM-FUTURE | Monitor for class support |
| `pydantic/logfire` | MEDIUM | Use OpenTelemetry + Jaeger instead |
| `pydantic/jiter` | LOW | Premature optimization |

### Key Findings

1. **MemRL already supports dynamic target selection** — contrary to initial assumption, the system stores and retrieves specific escalation targets (e.g., `"escalate:worker->coder_primary"`)

2. **Format parsing bug discovered and fixed** — `suggested_role` was returning `"worker->coder_primary"` instead of just `"coder_primary"`, breaking `_resolve_role()`

3. **Explicit graphs complement MemRL** — graphs define static edges (what transitions are valid), MemRL provides dynamic weights (which transition is best)

4. **Parallel workers ≠ parallel branching** — current fan-out uses `asyncio.gather()`, would remain inside nodes; explicit graphs handle conditional branching

### Bug Fix

**File**: `src/failure_router.py:324-336`
**Issue**: Arrow format `"escalate:from->to"` not parsed correctly for target extraction
**Fix**: Added `->` detection and right-side extraction
**Tests**: 3 new tests in `TestLearnedEscalationParsing` (57 total tests pass)

### Files Created

| File | Purpose |
|------|---------|
| `docs/diagrams/orchestration_topology.md` | Mermaid diagrams of routing graph |
| `handoffs/active/pydantic-ecosystem-migration.md` | Comprehensive migration handoff |

### Files Modified

| File | Change |
|------|--------|
| `src/failure_router.py` | Fixed arrow format parsing |
| `tests/unit/test_failure_router.py` | Added 3 tests for format parsing |

### Decision: Migrate to Explicit Graphs

**Rationale**:
- Single source of truth for routing topology
- Type-safe transitions (Union return types)
- Built-in state persistence for conversation resume
- Step-by-step debugging capability
- Self-documenting via Mermaid generation

**Migration Path** (in handoff):
1. Phase 2: POC with worker→coder→architect chain
2. Phase 3: MemRL integration via `ctx.deps`
3. Phase 4: State persistence backend
4. Phase 5: Full migration

### Test Results
- 57/57 tests pass in `test_failure_router.py`
- Arrow format parsing verified with new tests

---

## Pydantic Graph Migration — Full Implementation (Session 3-4)

### Summary
Implemented the full pydantic-graph migration plan: created graph module (7 node classes, persistence, mermaid), replaced the manual REPL loop in `repl_executor.py` with `run_task()`, removed FailureRouter/RoutingFacade, rewrote all affected tests. Fixed 5 bugs in the process.

### Bug Fixes
1. `escalation_count` never incremented → now incremented in `_handle_error()`
2. `record_failure()` never called → now called on every error
3. `record_mitigation()` never called → now called when escalation resolves
4. `add_evidence()` never called → now called on task outcomes
5. 3 hardcoded `EscalationPolicy()` fallbacks eliminated

### Files Created (10)
| File | Purpose |
|------|---------|
| `src/graph/__init__.py` | Public exports |
| `src/graph/state.py` | TaskState, TaskDeps, TaskResult, GraphConfig |
| `src/graph/nodes.py` | 7 node classes (Frontdoor, Worker, Coder, CoderEscalation, Ingest, Architect, ArchitectCoding) |
| `src/graph/graph.py` | orchestration_graph, run_task(), iter_task(), generate_mermaid() |
| `src/graph/persistence.py` | SQLiteStatePersistence adapter |
| `src/graph/_compat.py` | Bridge helpers for old API callers |
| `tests/unit/test_graph_nodes.py` | 24 node unit tests |
| `tests/unit/test_orchestration_graph.py` | 6 integration tests |
| `tests/unit/test_graph_persistence_adapter.py` | 9 persistence tests |
| `tests/unit/test_graph_mermaid.py` | 8 mermaid tests |

### Files Modified (10)
| File | Change |
|------|--------|
| `pyproject.toml` | Added `pydantic-graph` dependency |
| `src/api/routes/chat_pipeline/repl_executor.py` | Replaced manual for-loop with `run_task()` |
| `src/api/state.py` | Removed failure_router/routing_facade fields |
| `src/api/__init__.py` | Removed FailureRouter/RoutingFacade initialization from lifespan |
| `src/api/services/memrl.py` | Removed LearnedEscalationPolicy wiring |
| `src/api/routes/chat.py` | Replaced `routing_facade.decide()` with `EscalationPolicy().decide()` |
| `src/prompt_builders/builder.py` | Updated type hints, removed FailureContext/RoutingDecision |
| `src/prompt_builders/code_utils.py` | Updated imports from failure_router to escalation |
| `tests/unit/test_repl_executor.py` | Rewrote 23 tests to mock `run_task` |
| `tests/unit/test_generation_monitor.py` | Rewrote integration tests |

### Files Deleted (4)
| File | Replacement |
|------|-------------|
| `src/failure_router.py` | `src/graph/nodes.py` |
| `src/routing_facade.py` | `src/graph/nodes.py` |
| `tests/unit/test_failure_router.py` | `tests/unit/test_graph_nodes.py` |
| `tests/unit/test_routing_facade.py` | Graph integration tests |

### Test Results
- 47/47 graph-specific tests pass
- 2677/2677 total unit tests pass, 0 failures
- `make gates` — no new failures (shellcheck pre-existing)

---

## `/refactor` Slash Command — Codebase Refactoring Analysis (Session 5)

### Summary
Designed and implemented a `/refactor` slash command that analyzes a code scope for technical debt and produces a recurring refactoring handoff. Started from a ~500-line enterprise consulting prompt template, stripped it down to a focused 267-line command aligned with project conventions.

### Design Decisions

1. **Slash command, not agent definition** — existing `agents/` files were never used; a command in `.claude/commands/` integrates directly with Claude Code's `/` invocation
2. **Recurring handoff** — output goes to `handoffs/active/refactoring.md` which is overwritten (never archived) each cycle, creating a continuous improvement loop
3. **Analysis only, no implementation** — the command produces an actionable handoff for a separate implementation session
4. **Risk-weighted prioritization** — `Priority = (Severity × Freq × Risk_Multiplier) / Effort` where Risk_Multiplier comes from test coverage analysis (1.0/1.5/2.0)
5. **Phase 0 safety net** — untested code gets tests added before refactoring in later phases
6. **Test & coverage analysis** integrated as Step 4 — maps source→test coverage, test quality, test-to-source coupling from git history, CI/gate constraints

### Prompt Review Findings (from user's draft)
- Enterprise framing removed (team turnover, GDPR, Gantt charts, budget)
- 10-section consulting deliverable replaced with handoff format
- XML wrapper tags replaced with Markdown sections
- Scope narrowed from security+performance+compliance+refactoring to refactoring only
- "Not a code rewriter" constraint reframed: agent produces actionable handoff with code sketches, not just generic advice

### Analysis Pipeline (8 steps)
1. Scope Discovery — file inventory, line counts, import graph, public API surface
2. Complexity Analysis — structural complexity, duplication, dead code, naming
3. Dependency & Coupling — circular imports, god modules, shotgun surgery, layer violations
4. Test & Coverage Analysis — coverage mapping, test quality, git coupling, CI constraints
5. Risk-Prioritized Issue List — scored with test coverage risk multiplier
6. Design Refactoring Plan — phased, independently deployable, dependency-aware
7. Write Handoff — to `handoffs/active/refactoring.md` using project template
8. Validate — verify referenced files exist, run gates

### Files Created
| File | Purpose |
|------|---------|
| `.claude/commands/refactor.md` | `/refactor` slash command (267 lines) |

### Workflow
```
/refactor {scope} → review handoff → implement in separate session → repeat
```

### Next Steps
- Dry-run `/refactor` on a real scope (e.g., `src/api/routes`) to validate output quality
- Consider adding the recurring handoff concept to `handoffs/README.md`

---

## Smoke Test & Documentation Update (Session 6)

### Summary
Verified pydantic-graph migration against live model servers (3/3 smoke tests pass). Fixed a `final_answer` extraction bug discovered during live testing. Updated 8 documentation files to reflect the new architecture.

### Smoke Test Bug Fix
**Issue**: `_execute_turn()` returned `result.output` (empty for `FINAL()` calls) instead of `result.final_answer`. Live API returned `[Max turns reached without FINAL()]` even though the model correctly generated `FINAL("2 + 2 equals 4.")`.

**Fix**: In `src/graph/nodes.py:_execute_turn()`, prefer `result.final_answer` over `result.output` when `is_final=True`.

### Smoke Test Results

| Test | Answer | Turns | Elapsed | Route |
|------|--------|-------|---------|-------|
| "What is 2+2?" | `4` | 1 | 0.65s | frontdoor |
| "Write factorial function" | Full Python function | 1 | 4.87s | frontdoor |
| "TCP vs UDP" | 2-sentence explanation | 1 | 2.97s | frontdoor |

### Documentation Updated (8 files)

| File | Change |
|------|--------|
| `docs/chapters/18-escalation-and-routing.md` | Replaced RoutingFacade/FailureRouter with pydantic-graph |
| `docs/chapters/10-orchestration-architecture.md` | Added graph implementation note |
| `docs/chapters/15-memrl-system.md` | Documented newly-wired MemRL functions |
| `CLAUDE.md` | Updated Component Flow section |
| `src/escalation.py` | Updated comments |
| `src/roles.py` | Updated comment |
| `src/README.md` | Replaced failure_router with graph module |
| `agents/AGENT_INSTRUCTIONS.md` | Updated escalation guidance |

### Full Changeset Stats
- 687 lines added, 2,988 lines deleted (net -2,301 lines)
- 10 files created, 14 files modified, 4 files deleted
- 70 targeted tests pass, 2677 total unit tests pass
