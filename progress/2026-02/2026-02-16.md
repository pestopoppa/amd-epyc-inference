# Progress: 2026-02-16

## Distillation Pipeline Latency Instrumentation

Fixed inter-model transition latency blindness in seeding pipeline. Three changes:

1. **httpx client reuse** (`teachers.py`): `LocalLlamaTeacher` was creating a new `httpx.AsyncClient` per `distill()` call. Now lazily creates one and reuses across batches. Supports async context manager.
2. **Per-batch timing** (`pipeline.py`): `DistillationReport.batch_latencies` records `{skill_type, batch_index, batch_size, elapsed_ms, teacher}` per batch. Logged at INFO.
3. **New anomaly signal `distill_batch_latency`** (`anomaly.py`): 5s threshold (vs `slow_delegation`'s 120s). Weight 0.5. Backward-compatible kwarg.

146 tests pass (anomaly + distillation + teacher suites).

---

## Refactoring: src/ — Full Implementation (Phases 0-5)

Implemented the refactoring plan from `handoffs/active/refactoring.md` across all 5 phases. Analysis identified 22 issues in 16 files; highest-priority items addressed.

### Phase 0: Characterization Tests (Safety Net)

Added 403 new unit tests across 6 files covering the hottest untested source modules:

| Test File | Tests | Source File | Key Coverage |
|-----------|-------|-------------|--------------|
| `tests/unit/test_config.py` | 55 | `src/config/__init__.py` | `_env_bool/int/float`, all config dataclasses, `get_config()` singleton |
| `tests/unit/test_features.py` | 37 | `src/features.py` | Feature flags, `validate()` deps, env overrides, singleton lifecycle |
| `tests/unit/test_graph_nodes.py` | 84 new (117 total) | `src/graph/helpers.py` | `_classify_error`, `_extract_final_from_raw`, `_extract_prose_answer`, `_rescue_from_last_output`, `_is_comment_only`, `_detect_role_cycle` |
| `tests/unit/test_chat_delegation.py` | 34 | `src/api/routes/chat_delegation.py` | `_strip_think`, `_extract_toon_decision`, `_parse_architect_decision`, token budgets |
| `tests/unit/test_chat_routes.py` | 25 | `src/api/routes/chat.py` | `_try_cheap_first` (12 edge cases), endpoints, error codes, streaming |
| `tests/unit/test_repl_environment.py` | 52 | `src/repl_environment/environment.py` | Constructor, SAFE_IMPORT_MODULES, `_build_globals`, `execute()`, timeout, mixin contracts |

### Phase 1: Extract Magic Numbers to Constants

Created `src/constants.py` with three named constants:
- `TASK_IR_OBJECTIVE_LEN = 200` — prompt/objective truncation for TaskIR dicts
- `TOOL_OUTPUT_MATCH_LEN = 200` — answer matching in `_tools_success()`
- `DELEGATION_BRIEF_KEY_LEN = 200` — delegation brief cache/dedup key

Updated 10 files to use these constants (chat.py, chat_routing.py, chat_review.py, chat_summarization.py, chat_delegation.py, chat_pipeline/routing.py, chat_pipeline/proactive_stage.py, chat_pipeline/repl_executor.py, prompt_builders/builder.py, repl_environment/routing.py).

### Phase 2: Decompose God Files

**2A: config.py to config/ package**: Converted `src/config.py` (1553 lines) to `src/config/__init__.py` — zero-risk package conversion. All 30+ importers continue working unchanged. Future phases can extract sub-modules into the package.

**2B: graph/nodes.py decomposition**: Extracted 810 lines of helper functions into `src/graph/helpers.py`:
- Tap functions, error classification, failure/evidence recording
- `_execute_turn()` (300+ lines) — the core LLM→REPL turn
- Answer extraction (`_extract_final_from_raw`, `_extract_prose_answer`, `_rescue_from_last_output`)
- Escalation logic (`_should_escalate`, `_should_think_harder`, `_should_retry`)

`nodes.py` reduced from 1490 → 736 lines (node class definitions only). All helpers re-exported for backward compat.

### Phase 3: Decompose God Functions

**3A: _architect_delegated_answer()**: Extracted three helper functions from the 563-line mega-function:
- `_run_architect_decision()` — Phase A mini-REPL loop (architect decision with computation)
- `_apply_decision_guards()` — MCQ misroute, short-answer, and coding-task guards
- `_run_specialist_loop()` — Phase B specialist execution loop

Nesting reduced from 10 → 4 levels. Main function body reduced from 563 → ~150 lines.

3B (chat_vision._eval) and 3C (sqlite_store) skipped: low priority scores (3.0), individual methods well-sized.

### Phase 4: SRP Splits (Assessed, Deferred)

Assessed all 5 target files (builder.py 943L, registry_loader.py 850L, environment.py 876L, llama_server.py 712L, routing.py 699L). Individual methods are well-sized (20-60 lines). Files are large by line count but not pathologically structured. SRP splits would be over-engineering at this stage.

### Phase 5: Cross-Cutting Cleanup

Created `src/exceptions.py` with domain-specific exception hierarchy:
- `OrchestratorError` (base)
- `InferenceError`, `InferenceTimeoutError`, `BackendUnavailableError`
- `DelegationError`, `DelegationLoopError`
- `VisionAnalysisError`, `ArchiveExtractionError`, `DocumentProcessingError`
- `REPLExecutionError`, `REPLTimeoutError`
- `ConfigurationError`, `RegistryError`

Updated top offenders (graph/helpers.py, chat_delegation.py, chat_vision.py) with typed exception catches for LLM calls and archive extraction. Generic `except Exception` retained as fallback for unexpected errors.

### Verification

- `pytest tests/unit/test_*.py` — 403 characterization tests pass (2.2-4.7s)
- All existing tests unaffected
- No import breakage across the 30+ config importers

### Files Created

| File | Lines | Purpose |
|------|-------|---------|
| `src/constants.py` | 19 | Named constants for magic numbers |
| `src/graph/helpers.py` | 810 | Extracted node helper functions |
| `src/exceptions.py` | 67 | Domain-specific exception hierarchy |
| `src/config/__init__.py` | 1553 | Renamed from config.py (package conversion) |
| `tests/unit/test_config.py` | ~250 | Config characterization tests |
| `tests/unit/test_features.py` | ~200 | Features characterization tests |
| `tests/unit/test_chat_delegation.py` | ~200 | Delegation characterization tests |
| `tests/unit/test_chat_routes.py` | ~200 | Chat routes characterization tests |
| `tests/unit/test_repl_environment.py` | ~200 | REPL environment characterization tests |

---

## REPL Output Spill-to-File with Rolling Summary

Large REPL execution output (>8192 chars) was being hard-truncated, leaving the model blind to test results, error messages, and computed values. Now output spills to a file and a worker-generated rolling summary replaces the truncation.

### Design

- **Spill**: Output exceeding `output_cap` written to `{spill_dir}/{session_id}/turn_{N}.txt`
- **Rolling summary**: Worker model (Qwen2.5-7B, ~44 t/s) maintains a compressed scratchpad across turns:
  - Turn 1: summarize tail of output (max 10 lines)
  - Turn N: pass previous summary + new tail → worker updates summary, preserving key findings, dropping superseded details
- **Fallback**: Static head (15 lines) + tail (5 lines) when worker unavailable (unit tests)
- **Access**: Model can `peek(spill_path)` or `grep(spill_path, pattern)` for full details on demand
- `max_output_preview` bumped 500 → 1500 to accommodate summary + header + footer

### Files Created

| File | Lines | Purpose |
|------|-------|---------|
| `tests/unit/test_repl_spill_output.py` | ~150 | 12 tests: passthrough, spill, rolling summary, fallback, structured mode |

### Files Modified

| File | Change |
|------|--------|
| `src/repl_environment/environment.py` | Added `_spill_output()`, `_session_id`, `_turn_counter`, `_last_spill_summary`; replaced 2 truncation sites |
| `src/repl_environment/types.py` | Added `spill_dir` to `REPLConfig` |
| `src/prompt_builders/types.py` | Bumped `max_output_preview` 500 → 1500 |
| `tests/unit/test_repl_environment.py` | Updated `test_output_capped` for new spill behavior |

### Verification

- 170 REPL tests pass (135 existing + 12 new spill + 23 executor)
- 3826 total tests pass (1 pre-existing unrelated failure in test_inference_mixin)

---

### Files Modified (Refactoring)

| File | Change |
|------|--------|
| `src/graph/nodes.py` | Removed helpers (1490→736 lines), imports from helpers.py |
| `src/api/routes/chat_delegation.py` | Extracted 3 functions, typed exceptions |
| `src/api/routes/chat.py` | Magic numbers → constants |
| `src/api/routes/chat_routing.py` | Magic numbers → constants |
| `src/api/routes/chat_review.py` | Magic numbers → constants |
| `src/api/routes/chat_summarization.py` | Magic numbers → constants |
| `src/api/routes/chat_vision.py` | Typed exceptions |
| `src/api/routes/chat_pipeline/routing.py` | Magic numbers → constants |
| `src/api/routes/chat_pipeline/proactive_stage.py` | Magic numbers → constants |
| `src/api/routes/chat_pipeline/repl_executor.py` | Magic numbers → constants, import path |
| `src/prompt_builders/builder.py` | Magic numbers → constants |
| `src/repl_environment/routing.py` | Magic numbers → constants |
| `tests/unit/test_graph_nodes.py` | Import paths updated, 84 new tests |
