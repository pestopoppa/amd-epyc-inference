# Progress: 2026-02-15

## Phase 2A: SoftMatcha v2 Corpus-Augmented Prompt Stuffing

Implemented the full Phase 2A pipeline from `handoffs/active/hybrid-lookup-spec-decode.md`. This injects corpus-retrieved code snippets into LLM prompts so llama-server's `--lookup` has richer n-gram material for draft token proposals.

### New Files

- `scripts/corpus/build_index.py`: Collects .py files from src/, CPython stdlib, numpy, torch. Builds word-level 4-gram index. 73,186 snippets, 5.5M n-grams, builds in 14s. Index at `/mnt/raid0/llm/cache/corpus/mvp_index/` (338MB).
- `src/services/corpus_retrieval.py`: `CorpusRetriever` singleton service with `CorpusConfig`, `CodeSnippet`, `extract_code_query()`. Lazy-loads index, sub-ms query, graceful degradation if index missing.
- `tests/unit/test_corpus_retrieval.py`: 20 tests across 8 classes — singleton lifecycle, retrieval, dedup, format budget, graceful degradation.

### Modified Files

- `src/prompt_builders/types.py`: Added `reference_code` field to `RootLMPrompt`, rendered as `## Reference Code` section before `## Task`.
- `src/prompt_builders/builder.py`: Added `corpus_context` param to `build_root_lm_prompt()` (class + module-level); added `build_corpus_context()` function.
- `src/api/routes/chat.py`: Inject corpus_ctx on turn 0.
- `src/api/routes/chat_pipeline/stream_adapter.py`: Same pattern.
- `src/graph/nodes.py`: Inject corpus_ctx on first turn (`state.turns == 1`).
- `src/registry_loader.py`: Added `corpus_retrieval: bool` to `AccelerationConfig`, parsed from registry YAML.
- `orchestration/model_registry.yaml`: Added `corpus_retrieval` config block under `runtime_defaults` (enabled: false).
- `src/model_server.py`: Added `n_tokens_drafted`, `n_tokens_accepted`, `acceptance_rate` to `InferenceResult`.
- `src/backends/llama_server.py`: Extract acceptance rate telemetry from llama.cpp timings (both sync + streaming paths).
- `tests/unit/test_prompt_builders.py`: 7 new tests for `corpus_context` param and `build_corpus_context()`.

### Verification

- `pytest tests/unit/test_corpus_retrieval.py` — 20/20 pass
- `pytest tests/unit/test_prompt_builders.py` — 124/124 pass
- `pytest tests/` — 3981 pass, 15 pre-existing failures (distillation async + integration timeouts)
- `make schema shellcheck format lint` — all pass
- Feature is **OFF by default** (`corpus_retrieval.enabled: false`)

### A/B Test Results (Manual, MVP Corpus)

Tested corpus-augmented prompt stuffing on all 5 active models. Two task types per model (BST = binary search tree implementation, HTTP = async HTTP client). Corpus: 73K snippets from src/ + stdlib + numpy + torch.

| Model | Task | Accept Base→Corpus | Speed Base→Corpus | Wall Delta | Verdict |
|-------|------|-------------------|-------------------|------------|---------|
| Qwen2.5-Coder-32B (8081) | BST | 84.6%→93.3% (+8.7pp) | 30.8→32.7 t/s (+6%) | +2.8s | **Positive** |
| Qwen3-Coder-480B (8084) | BST | 74.9%→90.5% (+15.6pp) | 8.3→9.7 t/s (+17%) | -1.5s | **Best** |
| Qwen3-Coder-480B (8084) | HTTP | 68.1%→71.5% (+3.4pp) | 6.6→7.2 t/s (+9%) | +1.1s | **Positive** |
| Qwen3-235B-A22B (8083) | HTTP | 16.2%→22.8% (+6.6pp) | 5.9→6.0 t/s (+2%) | +3.9s | Marginal |
| Qwen2.5-7B (8082) | HTTP | 94.2%→99.5% (+5.3pp) | 80.3→81.4 t/s (+1%) | -2.4s | Saturated |
| Qwen3-Coder-30B (8080) | BST | 68.6%→70.7% (+2.1pp) | 53.4→47.2 t/s (-12%) | +2.5s | **Negative** |
| Qwen3-235B-A22B (8083) | BST | 60.7%→48.6% (-12.1pp) | 10.0→8.3 t/s (-17%) | +19.8s | **Negative** |

**Key findings:**
- Coder-family models benefit most (32B: +8.7pp, 480B: +15.6pp on BST task)
- 480B showed the best overall improvement: +17% speed, wall time *decreased*
- 30B: acceptance improved marginally but overhead from extra prompt tokens > speed gain
- 235B: mixed — positive on HTTP task, strongly negative on BST task
- 7B: already at 94-100% acceptance baseline, corpus adds negligible improvement
- Decision: Enable corpus for 32B and 480B (Coder family). Leave off for 30B, 235B, 7B.

### Bug Fixes

- **Telemetry key fix**: `src/backends/llama_server.py` used wrong keys (`drafted_n_tokens` → `draft_n`, `drafted_n_accepted` → `draft_n_accepted`). Both sync and streaming paths fixed.
- **Token normalization fix**: N-grams in index included punctuation but query n-grams didn't. Added `_normalize_token()` that strips non-alphanumeric chars (except underscore) across build_index.py, build_index_v2.py, corpus_retrieval.py, and test fixtures.

### Corpus Scaling: The Stack v1

- The Stack v2 is metadata-only (content IDs → Software Heritage). Used The Stack v1 (`bigcode/the-stack`) which has actual `content` field.
- Built `scripts/corpus/build_index_v2.py`: SQLite backend, HuggingFace streaming, multi-language support (Python, JS, TS, Rust, Go, C++), `--resume` for interrupted builds.
- Python build in progress: 67GB+ DB, ~12M+ snippets from The Stack v1.
- Remaining languages (JS, TS, Rust, Go, C++) queued to run after Python completes.
- Built `scripts/corpus/prune_index.py`: Optional post-build pruning by snippet count or target DB size.
- SQLite uses mmap — ~200KB RAM per query regardless of DB size. Full corpus fine on our 1.13TB machine.

### Next Steps

1. **Full corpus build completion**: Python + 5 remaining languages from The Stack v1.
2. **Re-run A/B tests** with full corpus (expect higher acceptance rates from broader n-gram coverage).
3. **A/B quality benchmark** (HARD GATE): Claude-as-Judge scoring. Quality regression > -0.5 is a blocker.
4. **Update model_registry.yaml** `index_path` to point to full_index once built.
5. **Phase 2B** (future): Sidecar draft injection in llama.cpp — only if Phase 2A shows >10% acceptance improvement.

### SoftMatcha v2 Notes

- Installed v0.1.0 from GitHub. PyICU dependency optional (only needed for Japanese tokenization).
- The library is designed for fuzzy/soft pattern matching with GloVe embeddings. For code (`exact_only: true`), we built a simpler word-level n-gram index as the default backend. SoftMatcha can be used as an optional upgrade for documentation corpus with soft matching.

## Qwen3-TTS Phase 4: llama.cpp Native C++ Pipeline

Built custom `llama-tts-qwen3` binary in `llama.cpp-experimental` (branch: `feature/qwen3-tts-support`).

### Architecture

Three-model pipeline: Talker GGUF (Q4_K_M, 462MB) + Code Predictor GGUF (Q8_0, 395MB) + sidecar weights (float32, 1.5GB). Sidecar holds text_embedding, text_projection MLP, codec_embedding, 15 CP embeddings, 15 CP lm_heads.

### What Works

- Binary compiles, loads all models, generates 16 codec tokens per frame
- Multi-head Code Predictor with correct per-step lm_head (via `llama_set_embeddings` + `llama_get_embeddings_ith`)
- Talker hidden state extraction for CP `past_hidden` input
- Temperature/top-k/repetition-penalty sampling
- Stale-frame EOS detection heuristic
- End-to-end pipeline: C++ binary → codec tokens → Python Tokenizer Decoder → WAV (24kHz)
- Codec generation speed: 1.5x real-time (vs PyTorch 0.9x RT)

### What's Broken: Audio Output is Noise

**Whisper transcription of 4s output**: "Assistant. Hello? This is the day of assistance!" (expected: "Hello, this is a test of the text to speech system.")

Root cause NOT yet identified. Suspects:
1. **Chat template leaking**: "assistant" heard in output suggests `<|im_start|>assistant\n` prefix is being spoken
2. **Text embedding/projection mismatch**: CPU float32 text_projection may not match PyTorch bf16 computation
3. **Codec embedding sum formula wrong**: The way 16 code embeddings + text hidden are combined for next Talker input may diverge from reference
4. **Wrong prompt format**: Talker may need different formatting than `<|im_start|>assistant\n{text}<|im_end|>\n<|im_start|>assistant\n`

### Bugs Fixed (6 total)

1. Sidecar header size mismatch (C 36B vs Python 32B)
2. CP vocab size mismatch (2048 not 3072)
3. llama_kv_cache_clear API removed in upstream
4. Logging not initialized
5. Single lm_head for all CP steps → multi-head via embeddings API
6. CP past_hidden using codec_embedding → now uses Talker hidden states

### Next Step

Generate PyTorch reference codec tokens for same text, compare token-by-token against C++ output to find divergence point. This will isolate whether the issue is in text projection, embedding injection, generation loop, or prompt format.

### Files

- `llama.cpp-experimental/tools/tts-qwen3/tts-qwen3.cpp` — main binary
- `llama.cpp-experimental/tools/tts-qwen3/CMakeLists.txt` — build config
- `scripts/voice/create_tts_sidecar.py` — sidecar generator (v2 format, QWTTS02)
- `scripts/voice/validate_tts_e2e.py` — end-to-end validation pipeline
- `scripts/voice/tts_server.py` — Phase 3 PyTorch server (reference implementation)
