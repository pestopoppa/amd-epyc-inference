# Progress Log: 2026-02-01

## Summary

**Vision → Document Pipeline Integration.** Wired `/chat` vision requests through the full document processing pipeline (DocumentPreprocessor, DocumentChunker, FigureAnalyzer, DocumentREPLEnvironment) instead of the old bare `ocr_image()` + VL direct-answer path that was failing silently. All 1234 tests pass.

---

## Session 1: Wire Vision Requests Through Document Pipeline + REPL

### Problem

VL OCR tests all fail in phase 3 baseline because `_execute_vision()` bypasses the document pipeline entirely. The full pipeline (PDFRouter, DocumentPreprocessor, DocumentChunker, FigureAnalyzer, DocumentREPLEnvironment) was built, tested (48 integration tests), and wired into `/documents` endpoints — but never integrated into the `/chat` flow.

The old `_execute_vision()`:
- Made a bare `client.ocr_image()` call that failed silently
- Hardcoded `tools_used=1` and `tools_called=["ocr_extract"]`
- Returned a single-shot VL model answer bypassing all orchestration

### Changes

5 files modified:

1. **`src/api/routes/chat_utils.py`** — Added `document_result: PreprocessingResult | None = None` to `RoutingResult` dataclass. This carries preprocessing results between pipeline stages.

2. **`src/api/routes/chat_pipeline.py`** — Two changes:
   - **`_execute_vision()`**: Completely rewritten. Now runs `DocumentPreprocessor` on images/files/base64, stores result on `routing.document_result`, returns `None` to fall through to REPL. On failure, injects context note and falls through.
   - **`_execute_repl()`**: When `routing.document_result` is present, creates `DocumentREPLEnvironment` (with `sections()`, `figures()`, `search_sections()` tools) instead of plain `REPLEnvironment`.

3. **`src/api/routes/chat.py`** — After `_execute_vision()` returns None, checks `routing.document_result`. If set, forces `execution_mode="repl"` and `initial_role=Role.FRONTDOOR`.

4. **`src/services/document_preprocessor.py`** — `_extract_document_paths()` now handles `type: "base64"` inputs. Decodes to bytes, detects format from magic bytes (PNG/JPEG/WebP/PDF), saves to temp file on `/mnt/raid0/llm/tmp/`.

5. **`src/api/routes/chat_vision.py`** — No code changes. Old functions (`_handle_vision_request`, `_vision_react_mode_answer`, `_handle_multi_file_vision`) kept for backward compatibility but no longer called from main `/chat` pipeline.

### Data Flow (New)

```
/chat + image → _execute_vision()
                  └─ DocumentPreprocessor.preprocess_file()
                       ├─ PDFRouter (pdftotext + PyMuPDF)
                       ├─ LightOnOCR (port 9001, bbox format)
                       ├─ DocumentChunker (markdown sections)
                       └─ FigureAnalyzer → VL model (figure descriptions)
                  → routing.document_result = PreprocessingResult
                  → return None (fall through)
              → chat.py forces execution_mode="repl", role=FRONTDOOR
              → _execute_repl() → DocumentREPLEnvironment
                  ├─ sections(): list section titles
                  ├─ section(n): get section n content
                  ├─ figures(): list figure metadata
                  ├─ search_sections(query): keyword search
                  ├─ document_info(): metadata dict
                  └─ REPL loop with text model synthesizes answer
```

### Test Results

```
1149 unit tests ............... PASS
  48 doc pipeline integration . PASS
  11 vision routing ........... PASS
  26 API tests ................ PASS
────────────────────────────────
1234 total                      0 failures
```

### Files Created/Modified

| File | Action |
|------|--------|
| `src/api/routes/chat_utils.py` | Modified — added `document_result` field |
| `src/api/routes/chat_pipeline.py` | Modified — rewrote `_execute_vision()`, modified `_execute_repl()` |
| `src/api/routes/chat.py` | Modified — force REPL for document results |
| `src/services/document_preprocessor.py` | Modified — base64 input support |
| `orchestration/progress/PROGRESS_2026-02-01.md` | Created |
| `progress/2026-02/2026-02-01.md` | Created |

---

## Session 2: Architecture Work Items WI-9, WI-10, WI-11


### Overview

Completed three architecture work items from the orchestration architecture roadmap, bringing total test count from 1234 to 1398 (0 failures).

### WI-9: Staged Reward Shaping (PARL-Inspired Annealing)

**Problem**: MemRL Q-scorer uses a fixed reward signal. PARL (arXiv:2402.03469) shows annealing from shaped→sparse rewards improves policy generalization.

**Changes**:
- Created `orchestration/repl_memory/staged_scorer.py` (~120 lines)
  - `StagedScorer` class with `lambda(step)` annealing from 0.3→0.0 over configurable horizon
  - Exploration bonus `1/sqrt(N+1)` encourages visiting underexplored routes
  - `compute_reward(base, shaping, step, visit_count)` composites all signals
- 8 unit tests in `tests/unit/test_staged_scorer.py`

### WI-10: Parallel Gate Execution

**Problem**: `GateRunner` executes gates sequentially. Schema + format + lint gates are independent and can run concurrently.

**Changes**:
- Modified `src/gate_runner.py`:
  - Added `_run_gate_parallel()` using `asyncio.to_thread()` for subprocess gates
  - Added `run_gates_parallel()` using `asyncio.gather()` for independent gates
  - Sequential fallback preserved for dependent gates
- `parallel_gates` feature flag in `src/features.py`
- 6 unit tests in `tests/unit/test_gate_runner.py` (including `AsyncMock` for awaitable mocks)

### WI-11: prompt_builders.py Decomposition

**Problem**: `src/prompt_builders.py` was a 1,501-line monolith mixing 7 distinct functional groups (types, constants, builder, review, code utils, error classification, formatting). Identified as S2 in architecture review.

**Solution**: Split into `src/prompt_builders/` package with 6 domain-focused sub-modules + `__init__.py` re-exports. Zero downstream import changes (17 importing files unchanged).

**Package Structure**:

```
src/prompt_builders/
  __init__.py      (~100 lines)  — Re-exports all 27 public names
  types.py         (~154 lines)  — PromptStyle, PromptConfig, RootLMPrompt, EscalationPrompt, StepPrompt
  constants.py     (~168 lines)  — Tool descriptions, rules, ReAct + vision whitelists, build_react_prompt()
  builder.py       (~383 lines)  — PromptBuilder class, compat functions, routing context, long context
  review.py        (~190 lines)  — Verdict, revision, plan review, architect delegation prompts
  code_utils.py    (~161 lines)  — extract_code, auto_wrap_final, classify_error
  formatting.py    (~57 lines)   — Format constraint patterns, detect_format_constraints, build_formalizer_prompt
```

**Design decisions**:
1. Package `__init__.py` re-exports pattern (same as `src/api/routes/` decomposition)
2. 6 modules by domain: each has single responsibility, minimal cross-module imports
3. `builder.py` keeps PromptBuilder class intact (methods share `self.config`)
4. Absolute imports (`from src.prompt_builders.types import ...`) for codebase consistency

### Test Results

```
1282 unit tests ............... PASS
  58 prompt_builders .......... PASS (all original tests)
  36 API import ............... PASS
  47 vision + plan review ..... PASS
────────────────────────────────
1398 total                      0 failures, 25 skipped
```

### Files Created/Modified

| File | Action |
|------|--------|
| `orchestration/repl_memory/staged_scorer.py` | Created (WI-9) |
| `tests/unit/test_staged_scorer.py` | Created (WI-9) |
| `src/gate_runner.py` | Modified (WI-10 — parallel execution) |
| `src/features.py` | Modified (WI-10 — `parallel_gates` flag) |
| `tests/unit/test_gate_runner.py` | Modified (WI-10 — 6 new tests) |
| `src/prompt_builders.py` | **Deleted** (WI-11) |
| `src/prompt_builders/__init__.py` | Created (WI-11) |
| `src/prompt_builders/types.py` | Created (WI-11) |
| `src/prompt_builders/constants.py` | Created (WI-11) |
| `src/prompt_builders/builder.py` | Created (WI-11) |
| `src/prompt_builders/review.py` | Created (WI-11) |
| `src/prompt_builders/code_utils.py` | Created (WI-11) |
| `src/prompt_builders/formatting.py` | Created (WI-11) |

---

## Session 3: Fix orchestrator_eval.py Pre-Launch Bugs

### Problem

Pre-launch review of `orchestrator_eval.py` found three bugs that would silently corrupt MemRL evaluation results:

1. **`mode` field missing from ChatResponse** — The Q-scorer builds action strings as `"role:mode"` (e.g., `"frontdoor:direct"`). Without a `mode` field, eval read `routing_strategy` instead, producing `"frontdoor:learned"` — fragmenting identical execution paths into different Q-values depending on whether MemRL or heuristics chose the route.

2. **`ORCHESTRATOR_REACT_MODE=1` missing from env vars** — Both `orchestrator_stack.py` and `orchestrator_eval.py` restart helpers set all feature flags except react mode. Without it, the heuristic fallback can never route to react mode during cold start.

3. **Direct mode lock-in** — `_should_use_direct_mode()` returns True for ~90% of benchmark prompts (anything with context ≤ 20KB and no file-operation keywords). Direct mode = single LLM call with no tools, no REPL, no self-escalation. MemRL never sees non-direct modes, so it can never learn when tools/escalation help.

### Changes

6 files modified:

1. **`src/api/models/responses.py`** — Added `mode: str = Field(default="", description="Execution mode: direct, react, repl, delegated, or mock")` to `ChatResponse`.

2. **`src/api/routes/chat_pipeline.py`** — Set `mode=` in all 6 `ChatResponse(...)` constructors:
   - `_execute_mock()` → `mode="mock"`
   - `_execute_delegated()` → `mode="delegated"`
   - `_execute_react()` → `mode="react"`
   - `_execute_direct()` → `mode="direct"`
   - `_execute_repl()` (two-stage) → `mode="repl"`
   - `_execute_repl()` (main) → `mode="repl"`

3. **`scripts/benchmark/orchestrator_eval.py`** — Changed `result.get("routing_strategy", "unknown")` to `result.get("mode", "unknown")`. Added `ORCHESTRATOR_REACT_MODE=1` to `restart_orchestrator_api()` env vars.

4. **`scripts/server/orchestrator_stack.py`** — Added `env["ORCHESTRATOR_REACT_MODE"] = "1"` to `start_orchestrator()`.

5. **`src/api/routes/chat_routing.py`** — Changed `_select_mode()` heuristic fallback from `direct → react → repl` to `react → repl`. REPL is a superset of direct (model can `FINAL("answer")` immediately for simple questions, or use tools if needed). This gives MemRL exposure to non-direct modes during cold start.

### Design Rationale

- **Why "repl" as default, not "direct"?** REPL mode is strictly a superset. The model can call `FINAL("answer")` on the first turn for simple questions (equivalent to direct), but it also has the *option* to use tools, escalate, or explore. MemRL learns from outcomes — if direct-style answering works, Q-values will converge to favor immediate FINAL(). But the model needs the opportunity to try tools first.
- **`_should_use_direct_mode()` kept but uncalled from `_select_mode()`**. The function is still defined and importable (test_api_imports.py validates this). It may be useful later when MemRL explicitly learns to choose direct mode.

### Test Results

```
83 tests ...................... PASS
  21 test_orchestrator_eval ... PASS
  26 test_api ................. PASS
  36 test_api_imports ......... PASS
──────────────────────────────────
83 total                        0 failures
```

### Files Modified

| File | Change |
|------|--------|
| `src/api/models/responses.py` | Added `mode` field to ChatResponse |
| `src/api/routes/chat_pipeline.py` | Set `mode=` in 6 ChatResponse constructors |
| `scripts/benchmark/orchestrator_eval.py` | Read `mode` instead of `routing_strategy`; added `ORCHESTRATOR_REACT_MODE=1` |
| `scripts/server/orchestrator_stack.py` | Added `ORCHESTRATOR_REACT_MODE=1` to env vars |
| `src/api/routes/chat_routing.py` | Heuristic default changed from "direct" to "repl" |

---

## Session 4: Post-Refactoring Architecture Cleanup (N1, N2, N3)

### Overview

Addressed three architecture issues identified in a comprehensive post-refactoring code health review. All three changes are non-functional (zero behavior change) except for N2 which hardens security.

### N1: Decompose repl_environment.py (3,511 lines -> 10-file package)

**Problem**: `src/repl_environment.py` was a 3,511-line monolith containing 11 classes and 53+ methods. The `REPLEnvironment` class alone had methods spanning file I/O, OCR, routing, delegation, procedures, context management, state inspection, and checkpoint/restore.

**Solution**: Mixin-based decomposition into `src/repl_environment/` package with 8 focused modules + `__init__.py` re-exports.

**Package Structure**:

```
src/repl_environment/
  __init__.py       (~50 lines)   — Re-exports all public symbols
  types.py          (~230 lines)  — Data types, exceptions, constants
  security.py       (~95 lines)   — ASTSecurityVisitor
  file_tools.py     (~720 lines)  — _FileToolsMixin: peek, grep, list_dir, archive, shell, patches
  document_tools.py (~230 lines)  — _DocumentToolsMixin: OCR, figure analysis
  routing.py        (~340 lines)  — _RoutingMixin: recall, escalate, delegate, route_advice
  procedure_tools.py(~270 lines)  — _ProcedureToolsMixin: procedures, checkpoints, registry, gates
  context.py        (~330 lines)  — _ContextMixin: chunking, FINAL signals, LLM wrappers, tool dispatch
  state.py          (~270 lines)  — _StateMixin: inspection, checkpoint/restore, exploration tracking
  environment.py    (~370 lines)  — REPLEnvironment (assembles 6 mixins), factory, _RestrictedREPLEnvironment
```

**Design**: Each domain is a mixin class. `REPLEnvironment` inherits all 6 via Python MRO. `__init__.py` re-exports all public names — zero downstream import changes (13 importing files).

### N2: Replace 5 shell=True subprocess calls with shell=False

**Problem**: Five `subprocess.run(cmd, shell=True)` calls in `src/` risked command injection.

**Changes**: All five converted to `subprocess.run(shlex.split(cmd))`:
- `src/model_server.py:309` — llama.cpp inference commands
- `src/repl_environment/file_tools.py:683` — REPL `run_shell()` (already allowlisted)
- `src/script_registry.py:451` — Script template execution
- `src/formalizer.py:223` — Formalizer CLI commands
- `src/gate_runner.py:206` — Gate execution (make, shellcheck)

Updated 7 test commands that used shell builtins (`exit 1` → `false`, `sleep && echo` → `sleep`, complex shell script → Python helper).

### N3: Delete dead src/api.py (1,852-line orphan)

**Problem**: Both `src/api.py` (file, 1,852 lines) and `src/api/__init__.py` (package) existed. Python resolves `src.api` to the package, making the file unreachable dead code.

**Fix**: Deleted `src/api.py`. Verified Python was already loading the package.

### Test Results

```
1419 tests .................... PASS
   0 failures                  0 regressions
  25 skipped, 4 xpassed, 140 warnings
```

### Files Created/Modified/Deleted

| File | Action |
|------|--------|
| `src/repl_environment.py` | **Deleted** (N1 — 3,511-line monolith) |
| `src/repl_environment/__init__.py` | Created (N1) |
| `src/repl_environment/types.py` | Created (N1) |
| `src/repl_environment/security.py` | Created (N1) |
| `src/repl_environment/file_tools.py` | Created (N1) |
| `src/repl_environment/document_tools.py` | Created (N1) |
| `src/repl_environment/routing.py` | Created (N1) |
| `src/repl_environment/procedure_tools.py` | Created (N1) |
| `src/repl_environment/context.py` | Created (N1) |
| `src/repl_environment/state.py` | Created (N1) |
| `src/repl_environment/environment.py` | Created (N1) |
| `src/model_server.py` | Modified (N2 — shlex.split) |
| `src/script_registry.py` | Modified (N2 — shlex.split) |
| `src/formalizer.py` | Modified (N2 — shlex.split) |
| `src/gate_runner.py` | Modified (N2 — shlex.split) |
| `tests/unit/test_gate_runner.py` | Modified (N2 — shell builtin → real commands) |
| `src/api.py` | **Deleted** (N3 — 1,852-line dead code) |

---

## Session 5: Post-Refactoring Hardening — Security, Thread Safety, Dead Code

### Overview

Applied 6 bounded hardening fixes identified during the post-refactoring architecture review (4 agents, 21+ issues found). Test count: 1419 → 1425 (6 new path validation tests).

### Fix 1: Path Traversal (Security — HIGH)

**Problem**: `documents.py:151` and `chat_vision.py:112` accepted user-supplied file paths via `Path(request.file_path)` without canonicalization. `../../` traversal bypassed the REPL's `_validate_file_path()` allowlist.

**Fix**: Created shared `src/api/routes/path_validation.py` with `validate_api_path()` — uses `os.path.realpath()` + allowlist check against `["/mnt/raid0/llm/", "/tmp/"]`. Mirrors the REPL pattern from `environment.py:220`.

### Fix 2: Base64 Size Limits (Security — HIGH)

**Problem**: `documents.py:162` and `chat_vision.py:145` decoded base64 payloads without size checks. Attacker could send multi-GB payloads to exhaust memory.

**Fix**: Added size guards before `base64.b64decode()` — 100MB max for documents, 50MB max for images. Returns HTTP 413 on overflow.

### Fix 3: Config Endpoint Auth (Security — CRITICAL)

**Problem**: `POST /config` accepted feature flag changes from any network client with no authentication.

**Fix**: Added localhost restriction — only `127.0.0.1`, `::1`, and `localhost` can call the endpoint. Returns HTTP 403 for remote clients.

### Fix 4: Singleton Thread Safety (Correctness — MEDIUM-CRITICAL)

**Problem**: 21+ module-level singletons used `if _x is None: _x = X()` without locks. Under concurrent FastAPI requests, duplicate instances could be created (critical for `WorkerPoolManager` which spawns subprocesses).

**Fix**: Added `threading.Lock()` with double-checked locking to 6 highest-risk singletons:
- `src/services/worker_pool.py` — `_pool_instance` (CRITICAL: spawns subprocesses)
- `src/api/routes/sessions.py` — `_session_store` (HIGH: SQLite init)
- `src/services/figure_analyzer.py` — `_analyzer`
- `src/services/document_chunker.py` — `_chunker`
- `src/services/document_client.py` — `_client`
- `src/services/draft_cache.py` — `_draft_cache`

### Fix 5: Dead Code Cleanup

- Deleted unused `role_enum = Role.from_string(self.role)` in `routing.py:180`
- Added deprecation docstring to `gradio_ui.py` (retained as optional entry point)
- Confirmed `cli_orch.py`, `cli.py`, `cli_sessions.py` are NOT dead (registered pyproject.toml entry points)

### Test Results

```
1425 tests .................... PASS
   6 new (path validation) ... PASS
   0 failures                  0 regressions
  25 skipped, 4 xpassed, 140 warnings
```

### Files Created/Modified

| File | Action |
|------|--------|
| `src/api/routes/path_validation.py` | Created — shared path validation helper |
| `src/api/routes/documents.py` | Modified — path traversal fix + base64 size limit |
| `src/api/routes/chat_vision.py` | Modified — path traversal fix + base64 size limit |
| `src/api/routes/config.py` | Modified — localhost-only restriction |
| `src/services/worker_pool.py` | Modified — thread-safe singleton |
| `src/api/routes/sessions.py` | Modified — thread-safe singleton |
| `src/services/figure_analyzer.py` | Modified — thread-safe singleton |
| `src/services/document_chunker.py` | Modified — thread-safe singleton |
| `src/services/document_client.py` | Modified — thread-safe singleton |
| `src/services/draft_cache.py` | Modified — thread-safe singleton |
| `src/repl_environment/routing.py` | Modified — removed unused variable |
| `src/gradio_ui.py` | Modified — deprecation docstring |
| `tests/unit/test_path_validation.py` | Created — 6 tests |
