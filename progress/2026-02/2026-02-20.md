# Progress Report — 2026-02-20

## ColBERT-Zero Research Integration

### Summary

Integrated findings from ColBERT-Zero paper (arXiv:2602.16609) into NextPLAID retrieval stack and MemRL pipeline. 4-step plan executed: literature review, documentation updates, GTE-ModernColBERT-v1 evaluation, MemRL distillation architecture design.

### Key Findings

- **Query/document prefixes**: ELIMINATED — LateOn-Code requires no prefixes. GTE-ModernColBERT-v1 uses `[Q]`/`[D]` prefixes handled automatically by NextPLAID container.
- **PLAID PQ compression**: CONFIRMED ACTIVE — `nbits=4` IVF+PQ hybrid already in `index_codebase.py:79`.
- **ONNX blocker**: RESOLVED — official INT8 export exists on HuggingFace (143MB). No custom conversion needed.

### GTE-ModernColBERT-v1 Docs Model Swap

Replaced `answerai-colbert-small-v1` (33M, 96-dim) with `GTE-ModernColBERT-v1` (149M, 128-dim) on `:8089`.

**A/B results (10 queries)**:
- 5 clearly better (speculative decoding → ch05:1, REPL tools → ch11:1, feature validation → correct handoff, session compaction → correct handoff, verification gates → ch10 correct section)
- 4 comparable
- 0 worse
- Latency: 28ms → 50ms (+78%, within <2x)

**Key details**:
- Official ONNX INT8: 143MB, 128-dim output with Dense projection bundled
- Uses `[Q] ` / `[D] ` prefixes (query_prefix_id=50368, document_prefix_id=50369)
- Reindexed 1992 doc chunks in 246s
- Memory: ~500MB (up from ~200MB)

### MemRL Distillation Architecture

Designed 3-stage pipeline mirroring ColBERT-Zero (unsupervised → supervised → distillation) for a fast routing classifier to bypass full TwoPhaseRetriever on routine decisions.

- Model: 4-layer classifier, ~10K params, ~120-dim input features
- Integration: HybridRouter fast path (confidence >= 0.6), retrieval fallback
- Key insight from paper: supervised stage before distillation bridges performance gap

### Files Created

| File | Purpose |
|------|---------|
| `research/colbert_zero_review.md` | Literature review |
| `handoffs/active/colbert-zero-research-integration.md` | Master handoff |
| `docs/reference/agent-config/MEMRL_DISTILLATION_DESIGN.md` | Distillation architecture |
| `progress/2026-02/2026-02-20.md` | This report |

### Files Modified

| File | Change |
|------|--------|
| `CHANGELOG.md` | Added 2026-02-20 entry |
| `orchestration/BLOCKED_TASKS.md` | New task entry, updated timestamp |
| `orchestration/model_registry.yaml` | Docs model → GTE-ModernColBERT-v1 |
| `docs/reference/models/QUIRKS.md` | ColBERT retrieval model quirks section |
| `docs/reference/benchmarks/RESULTS.md` | New Retrieval Stack section |

### Next Steps

- [ ] Run `make gates` to verify all checks pass
- [ ] Monitor docs search quality in production use
- [ ] PLAID parameter tuning (`n_ivf_probe`, `n_full_scores`) — noted for future
- [ ] MemRL distillation Phase 2: training data extraction script

---

## Feature Validation Battery: Live A/B Testing

### Summary

Completed live validation of 23 orchestrator features across tiers 0-3. Enabled 10 features in production defaults (`src/features.py`, commit `9b7f345`). Each feature tested via hot-reload (`POST /config`) with 5 prompts measuring latency delta against production baseline.

### Results

**Tier 1 — MemRL Incremental Chain (4/4 PASS)**:
- specialist_routing (-25.0s), plan_review (-24.8s), architect_delegation (-24.9s), parallel_execution (-25.5s)

**Tier 2 — Independent Features (5/8 PASS, 1 BORDERLINE, 2 FAIL)**:
- PASS: react_mode (-36.8s), output_formalizer (-21.3s), input_formalizer (-16.2s), unified_streaming (-7.9s), model_fallback (-1.5s)
- BORDERLINE (enabled per operator decision): escalation_compression (+4.8s, confirmed on rerun)
- FAIL: binding_routing (+6.5s), personas (+20.6s)

**Tier 3 — Safety & Infrastructure (5/6 PASS)**:
- PASS: approval_gates (-20.6s), cascading_tool_policy (-15.3s), side_effect_tracking (-28.3s), structured_tool_output (-8.1s), resume_tokens (-1.1s)
- FAIL: credential_redaction (+15.1s) — already enabled as safety feature, overhead accepted

### Methodology

- Offline validation (unit + replay): All tiers 0-3 pass (21/21)
- Live baseline capture → feature enable via `POST /config` → same prompts → compare → revert
- Borderline features rerun with raw response persistence (per-prompt status, tokens, routing, answers)
- 504 Gateway Timeouts are systemic (~8 per run), not feature-specific
- Quality delta 0.0 across all features (Claude-as-Judge not wired into live path)

### Files

| File | Change |
|------|--------|
| `src/features.py` | 10 features → True with validation dates |
| `README.md` | New "Active Production Features" section with chapter links |
| `handoffs/active/feature-validation-battery.md` | Updated with full results |
| `CHANGELOG.md` | Added 2026-02-20 entry |
| `orchestration/BLOCKED_TASKS.md` | Updated status |
| `benchmarks/results/runs/feature_validation/live/*.jsonl` | Per-feature results with raw responses |

### Next Steps

- [x] Complete tier 3 live validation (5/6 PASS)
- [x] Enable tier 3 passing features in production (commit `123c272`)
- [ ] Quality scoring pass over persisted raw responses (Claude-as-Judge)
- [ ] Archive handoff

---

## Slot/Admission Alignment + Backend Saturation Handoff

### Problem Discovered

Feature comparison benchmark (baseline vs candidate with 15 features) failed repeatedly with 504 Gateway Timeouts and 429 Too Many Requests. Failures clustered after prompt 5-7, worsening in candidate phase. Two runs attempted — neither produced clean comparison data.

### Root Cause: KV Cache Waste

Every backend had **2x more llama-server slots than admission allowed**. llama-server partitions KV cache evenly across all slots, so 50% of KV memory was idle. Worker (8082) was worst: 8 slots allocated, admission=4, but sweep showed even 2 concurrent was bad for p95. Each request got 1/8th of available KV.

### Fix Applied

Aligned server slots with admission limits based on `concurrent_sweep_20260219_144159.summary.json`:

| Backend | Old Slots → New | Old Admission → New | Sweep Result |
|---------|-----------------|---------------------|--------------|
| frontdoor (8080) | 4 → 2 | 2 (unchanged) | Optimal at 2 (p95 1.33x) |
| coder_escalation (8081) | 4 → 1 | 2 → 1 | p95 1.98x at concurrency=2 — serial only |
| worker (8082) | 8 → 1 | 4 → 1 | All concurrent levels rejected on p95 |
| architect_general (8083) | 2 → 1 | 1 (unchanged) | Already serial |
| architect_coding (8084) | 2 → 1 | 1 (unchanged) | Already serial |
| ingest (8085) | 2 → 1 | 1 (unchanged) | Already serial |

### Additional Fix (from earlier session)

Frontdoor timeout: 90s → 180s in `model_registry.yaml` (was never applied to running process).

### Files Modified

| File | Change |
|------|--------|
| `orchestration/model_registry.yaml` | Slot counts aligned + frontdoor timeout 90→180s |
| `src/api/admission.py` | Admission limits aligned with sweep results |
| `scripts/benchmark/feature_comparison.py` | 60s cooldown, 5s inter-prompt, 180s client timeout |
| `handoffs/active/backend-saturation-504-429.md` | NEW — full investigation handoff |

### Handoff Created

`handoffs/active/backend-saturation-504-429.md` contains:
- 6 hypotheses ranked by likelihood
- 6-step investigation playbook with shell commands
- Timeline of all prior hardening work (Feb 11 → Feb 20)
- Reproduction steps and acceptance criteria

---

## GraphRouter MemRL Augmentation

### Summary

Implemented GNN-based parallel routing signal for MemRL HybridRouter cold-start optimization. Based on GraphRouter (ICLR 2025, arXiv:2410.03834). Solves new model onboarding time: hours of organic data accumulation → minutes of scripted inductive prediction.

### Implementation (7 Phases, 49 Tests)

| Phase | File | Description |
|-------|------|-------------|
| A | `orchestration/repl_memory/routing_graph.py` | Kuzu bipartite graph: TaskType, QueryCluster, LLMRole nodes. MiniBatchKMeans clustering from EpisodicStore. PERFORMANCE_ON edges with success_rate, avg_q, avg_latency. |
| B | `orchestration/repl_memory/lightweight_gat.py` | Pure numpy 2-layer heterogeneous GAT (1024→32×4→32). Multi-head attention with scatter aggregation via `np.add.at`. Save/load `.npz`. |
| C | `orchestration/repl_memory/graph_router_predictor.py` | TTL-cached inference wrapper (<0.5ms warm). Finds nearest QueryCluster, runs GAT forward, softmax over LLM roles. |
| D | `orchestration/repl_memory/retriever.py` | HybridRouter: `graph_router` + `graph_router_weight` params. `_blend_graph_router_scores()` + adaptive weight (0.1→0.3 by store size). Telemetry in `_record_decision_meta`. |
| E | `src/features.py`, `src/api/services/memrl.py` | Feature flag `graph_router` (requires `specialist_routing`). Init in `ensure_memrl_initialized()`. |
| F | `scripts/graph_router/train_graph_router.py` | Offline training: sync graph, edge masking 20%, BCE loss, SGD with cosine LR, early stopping. |
| G | `scripts/graph_router/onboard_model.py` | Inductive new model onboarding: embed capability → LLMRole node → GAT predicts routing. |

### Architecture

```
HybridRouter.route(task_ir):
  ├── TwoPhaseRetriever (EXISTING, unchanged)
  ├── GraphRouterPredictor (NEW, parallel signal)
  │   └── Kuzu bipartite graph → 2-layer GAT → edge scores per role
  ├── Ensemble blending (NEW)
  │   └── posterior = (1-w) × retriever + w × graph_router
  │       w anneals 0.1→0.3 by episodic store size
  └── Fallback: RuleBasedRouter (EXISTING, unchanged)
```

### Key Design Decisions

- **Max 30% GNN influence**: LLMRouterBench found routing gains are coarse-grained. Conservative blend.
- **Pure numpy GAT**: No PyG/DGL dependency. ~130 nodes × 32-dim → <0.1ms forward pass.
- **Decoupled from seeding**: `seed_specialist_routing.py` unchanged. Training pipeline reads EpisodicStore offline.
- **Graceful degradation**: No weights / empty graph / not ready → silent skip, zero behavioral change.

### Test Summary

| Test File | Tests | Coverage |
|-----------|-------|----------|
| `test_routing_graph.py` | 9 | Schema, CRUD, sync, features, edges, cluster lookup |
| `test_lightweight_gat.py` | 22 | Activations, init, forward, edge prediction, loss, save/load |
| `test_graph_router_predictor.py` | 7 | Ready check, predict, caching, invalidation, graceful degradation |
| `test_graph_router_integration.py` | 7 | HybridRouter blend, not-ready skip, decision meta, adaptive weight |
| `test_graph_router_cold_start.py` | 4 | New model node, inductive prediction, empty graph, different models |

### Next Steps

- [ ] Run `python3 scripts/graph_router/train_graph_router.py` to produce GAT weights
- [ ] Run feature validation: `python3 scripts/benchmark/feature_validation.py --feature graph_router --tier 3`
- [ ] Test cold-start with `onboard_model.py` for a simulated new model
- [ ] Enable in production after validation passes

---

## OpenAI-Format Tool Call Translation Fix

### Problem

Qwen3-Coder-30B emits OpenAI-format JSON tool_calls when it wants to use REPL tools:
```
[{"id":"call_...","function":{"name":"web_search","arguments":"{\"query\":\"...\"}"},"type":"function"}]
```
instead of the expected REPL syntax:
```
result = CALL("web_search", query="...")
```

The REPL code extractor (`extract_code_from_response`) didn't recognize this format, so:
1. Tool calls were never executed
2. No tool results returned to the model
3. Model generated 20+ identical tool calls in a degenerate repetition loop
4. `\n```\n` stop sequence never fired (no code blocks generated)

Discovered during 3-way eval (`seed_specialist_routing.py --3way`) on GPQA Organic Chemistry question — SELF:repl hung for 3+ minutes generating identical `web_search` calls.

### Root Cause

Qwen3-Coder's instruct training on chat-completion tool-calling format overrides prompt instructions even in raw `/completion` mode. The model sees tool descriptions in the system prompt text and spontaneously generates structured JSON tool_calls instead of inline Python `CALL()` syntax. The llama-server uses `chat_format: Content-only` with raw `/completion` endpoint, so no chat template is applied — this is purely model behavior.

### Fix

Added `translate_openai_tool_calls()` in `src/prompt_builders/code_utils.py`:

1. **Detection**: Regex `_TOOL_CALL_RE` matches `"function":{"arguments"|"name"` or `"id":"call_..."` patterns
2. **Extraction**: `_extract_json_arrays()` — balanced-bracket scanner handles nested braces in `"arguments"` fields (simple regex `[^]]+` fails on nested JSON)
3. **Deduplication**: Identical tool calls (same name + args) collapsed to single invocation — kills the 20+ repetition loop
4. **Translation**: Converts to `result = CALL("web_search", query="...")` + `print(result)` — no auto-FINAL, model continues reasoning with tool output in next turn
5. **Integration**: Hooked as preprocessing step in `extract_code_from_response()` — runs before any other code extraction logic

Also added `CALL(` to `code_starters` list so properly-formatted CALL invocations are detected.

### Additional Fix: Stale Orchestrator Instance

Killed duplicate uvicorn orchestrator (PID 2557207, 8+ hours old, 6 workers at ~100% CPU each). The stale instance was starving llama-servers of CPU time, causing the frontdoor to generate tokens extremely slowly.

### Files Modified

| File | Change |
|------|--------|
| `src/prompt_builders/code_utils.py` | Added `_extract_json_arrays()`, `translate_openai_tool_calls()`, `CALL(` in code_starters |
| `src/prompt_builders/__init__.py` | Exported `translate_openai_tool_calls` |

### Test Results

- 129 existing tests pass (code_utils, prompt_builder, extract_code)
- Manual test: single call, multi-call with deduplication, mixed content, multiple args, no false positives on normal JSON

---

## Slot-Erase-on-Timeout + Delegation Timeout Fix

### Problem

During 3-way eval, the orchestrator hangs when:
1. ARCHITECT delegates to coder_escalation, which holds the inference lock for 40s+
2. Next question's SELF:direct (frontdoor) can't acquire the lock, times out after 15s
3. The coder_escalation backend keeps generating tokens nobody reads (resource leak)
4. No cancellation signal is sent to the backend
5. Delegated models get squeezed by parent's remaining deadline instead of their standard role timeout

### Fix 1: Slot Erase on Lock Timeout (`src/inference_lock.py`)

Ported `_erase_slots` logic from `scripts/benchmark/seeding_orchestrator.py` into production as `_erase_port_slots(port)`. Lightweight, fire-and-forget, caches working erase strategy per port.

- **On lock acquisition timeout**: `_lock_holder_ports()` reads `/proc/{pid}/cmdline` to map holder PIDs to llama-server ports, then erases processing slots on those ports
- **On error inside lock (finally block)**: erases slots on the current role's port to free it for the next request
- New `port` parameter on `inference_lock()` enables cleanup when provided

### Fix 2: Port Propagation (`src/llm_primitives/inference.py`)

- Added `_extract_port(url)` helper to parse port from backend URLs
- `_real_call_single()`, `_call_caching_backend()`, and `_real_call_monitored()` now pass `port=` to `inference_lock()`

### Fix 3: Delegation Timeout Budget (`src/api/routes/chat_delegation.py`)

Before calling `_run_specialist_loop`, wraps the call in `primitives.request_context(deadline_s=...)` with a deadline computed as `max(role_timeout, specialist_budget_s)`. This gives the specialist its full configured role timeout instead of being squeezed by the parent's remaining deadline. The specialist loop already enforces wall-clock limits via elapsed checks.

### Files Modified

| File | Change |
|------|--------|
| `src/inference_lock.py` | Added `_erase_port_slots()`, `_lock_holder_ports()`, `_SLOT_ERASE_CAPABILITY` cache. Added `port` param to `inference_lock()`. Timeout/error slot cleanup in except + finally. |
| `src/llm_primitives/inference.py` | Added `_extract_port()`. Pass `port=` to all `inference_lock()` calls. |
| `src/api/routes/chat_delegation.py` | Nested `request_context()` before `_run_specialist_loop` with extended specialist deadline. |

### Test Results

- 62 inference_lock tests: all pass (4 skipped)
- 140 delegation tests: all pass (3 pre-existing stale constant failures excluded)
- Import verification: all new functions accessible

### Fix 4: llama.cpp Server Slot Erase Endpoint (production-consolidated)

Initial 3-way eval revealed `slot erase unsupported on port 80XX; disabling erase attempts` on all ports. Two server-side issues:

1. **`--slot-save-path` gate blocked erase**: The `POST /slots/:id?action=erase` endpoint required `--slot-save-path` even though erase doesn't touch disk. Moved the check to only gate save/restore actions.

2. **Erase deferred on processing slots**: The erase handler deferred (queued for later) when a slot was `is_processing()`. Changed to force-release the processing slot immediately (`slot->release()`) then clear KV cache. This is the critical fix — without it, erase can't cancel actively-generating inference.

**Files modified (llama.cpp fork)**:
- `tools/server/server-context.cpp`: Two changes — erase allowed without `--slot-save-path`, force-release on processing slots

**Build**: `cmake --build build --target llama-server` — successful. All servers need restart to pick up the binary.

## REPL Tool Syntax Hints on Error

### Problem

When Qwen3-Coder-30B generates malformed tool calls (OpenAI JSON format or incorrect syntax), the REPL returns a generic `SyntaxError` or `TypeError` with no guidance on correct tool calling syntax. The model then abandons tool use entirely, falling back to writing reasoning in Python comment blocks instead of executing tools.

### Fix

Added `_tool_hint_if_relevant(code, exc)` method to `REPLEnvironment`. When a SyntaxError/Exception occurs and the code contains tool-call-like patterns, a hint is appended showing:
- Correct `CALL('tool_name', arg1=value1)` syntax
- Available tools from the tool registry
- Pointer to `list_tools()` for full details

Patterns detected: JSON `"function"/"name"` keys, direct tool name calls like `web_search(...)`, `tool_call` mentions, `"type": "function"`.

### Files Modified

| File | Change |
|------|--------|
| `src/repl_environment/environment.py` | Added `_tool_hint_if_relevant()`, injected in both structured and normal mode error handlers |

### Test Results

- 297/298 REPL tests pass (1 pre-existing stale checkpoint mock failure)

---

## Final wrap-up (integration + publish)

- Completed refactoring handoff implementation through Wave 2 (config package split) and verified targeted regressions in serial/mock-safe mode.
- Executed final end-of-handoff gate run: `make gates` passed.
- Consolidated parallel debugging changes (seed_specialist_routing + orchestrator-adjacent wiring) with refactor stream into one integration state.
- Prepared and published final integration commit to GitHub.
